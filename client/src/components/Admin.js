
const Admin = ({ userData }) => {
    // NOTE: left off here. was finna display all user data and allow for making edits to them
    return (
        <div>
            Hello from admin!
        </div>
    )
}

export default Admin